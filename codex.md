# 项目协作约束

1. 每次更新代码或配置之前，必须先获得用户明确同意；未经同意，不得擅自修改。
2. 代码必须保持可读的换行与缩进；不得将注解、方法签名和方法体等内容堆叠在同一行。
3. 回答必须基于现有代码、可验证事实或主流工程实践；不得为了顺从用户而给出不准确的结论。发现用户方案存在不完整、错误或风险时，必须明确说明依据、影响与可选方案。

## 整体架构与职责边界

项目由前端、Java 后端层和 Python Agent 层构成。各层必须职责单一，禁止跨层承担其他层的职责。

### 前端层

- 负责页面、交互和业务信息展示。
- 只能通过 Java 后端公开的接口获取和提交业务数据。
- 不承担业务规则、数据持久化或 Agent 执行职责。

### Java 后端层

- 负责业务逻辑、业务数据与对外业务接口，是前端与 Agent 层之间的业务协调者。
- 使用 `Controller -> Service -> Mapper` 分层：Controller 处理 HTTP 请求与参数校验；Service 实现业务编排、事务和调用 Agent；Mapper 负责数据访问。
- 实体类统一存放在 `entity` 目录，不与 Controller、Service 或 Mapper 混放。
- Java 后端只能通过定义明确的接口调用 Python Agent 服务，不得将 Agent 执行逻辑塞入 Java 业务层。

### Python Agent 层

- 负责构建并运行 Agent 服务，提供 Agent 执行能力供 Java 后端调用。
- 承担 Agent Loop、模型调用、RAG、记忆、工具与 Skills 等 Agent 域内职责。
- 不承担 Java 业务逻辑、前端展示或直接操作 Java 业务数据的职责。

## 代码编写约束

1. 编写代码时，应主动删除已过期、已被替代且不再被调用的代码；删除前必须确认不影响仍在使用的契约。
2. 每个函数必须附带函数级注释，说明其业务功能、实现思路；关键逻辑必须补充行级注释，说明其原因与约束。
3. 禁止为简单的格式转换、类型转换或单行包装操作单独定义函数。函数必须具备明确的业务功能。
4. 新增函数和方法必须遵循项目统一的 camelCase 命名方式，不使用下划线分隔名称。
5. 与业务无关的格式转换、数据变换或通用工具函数必须放在 `utils` 下，并按功能分目录；业务函数必须只表达明确的业务能力。
6. Java Service 可以引入新的可复用服务，但只有预计至少被两个业务场景复用时才允许拆分；不得为单一简单功能创建独立 Service。
7. 代码中的函数级注释、类注释和关键行注释必须使用中文。
8. 编写代码时不得以占位、删减路径、只写接口未落地逻辑等方式偷偷阉割已确定的功能。允许按模块逐步实现，但每次声明完成的功能必须具备完整可运行的业务闭环；若缺少关键信息无法实现，必须先向用户说明。
9. 所有生产提示词必须存放在 `Prompts` 目录中，并按 Agent、Memory、RAG、Skills 等功能域建立子目录。业务代码只能通过提示词加载服务读取模板，不得在代码中硬编码系统提示词、用户提示词或其他生产提示词内容。
10. Tools 目录只保留工具调用必需的参数检查、执行超时与真实安全边界（例如网页抓取的 SSRF 和资源上限防护）。不得加入无业务意义、与上游契约重复，或仅为了形式完整而存在的校验。
11. 撰写架构文档、README、设计说明和接口说明时，使用正向表述描述模块职责、能力、输入与输出边界；禁止使用“某模块不做什么”“某模块不负责什么”或同类否定式表述。

## 工程化边界

1. 禁止过度工程化。不得为了让架构“看起来完整”而增加没有实际业务价值的状态字段、版本字段、重复校验、缓存层、中间层、Service、事件或复杂框架。
2. Java 层只做必要的 `userId` 权限校验、业务流程控制、Java 侧数据持久化，以及对 Agent 调用结果的必要判断；不负责简历解析、文档切分、Embedding、RAG 检索、记忆处理或大模型编排。
3. 可靠性只优先保证本项目确实需要的能力：有限重试、幂等、关键数据一致性、服务失败时的降级和熔断。除非出现明确业务需求，不引入分布式事务、复杂状态机或完整服务治理平台。
4. 只有当状态会改变业务可用性、重试行为或用户可见结果时，才增加状态字段；同一含义不得在多个表中重复建模。
5. Redis 只用于短期缓存、幂等结果、必要的锁、限流和熔断计数；Redis 丢失后，核心业务必须能够从数据库或 Agent 持久化数据恢复。
6. 重试只针对网络超时、临时不可用等明确可恢复错误；权限错误、参数错误、资源不存在和明确的业务失败不得盲目重试。重试必须复用同一个幂等键或任务标识。
7. 不允许偷偷阉割功能。可以按模块逐步实现，但声称完成的功能必须形成真实业务闭环；无法实现时必须明确告知原因，不得用空实现伪装完成。
8. 不得为没有独立业务含义的简单操作单独定义函数，例如基础类型转换、单字段取值或单行包装；应直接在使用处完成。
9. 可识别的业务能力。只有预计会被至少两个服务复用的业务能力才允许抽取为公共函数或服务；仅被单处调用的简单逻辑应保留在调用处，避免无意义地拉长调用链。
